BEGIN TRY

BEGIN TRAN;

-- AlterTable
ALTER TABLE [dbo].[Escaneo] ADD [tipo] VARCHAR(20) NOT NULL CONSTRAINT [Escaneo_tipo_df] DEFAULT 'Entrada';

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH

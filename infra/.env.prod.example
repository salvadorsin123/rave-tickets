# Plantilla de secretos de PRODUCCION. Copiala a infra/.env.prod EN EL SERVIDOR y llena
# los valores ahi. infra/.env.prod esta en .gitignore: nunca debe llegar al repositorio.
#
# Genera cada secreto en el servidor con:  openssl rand -base64 48

# PostgreSQL
DB_PASSWORD=

# MinIO: credenciales root (solo para administrar el servicio con `mc`)
MINIO_ROOT_USER=
MINIO_ROOT_PASSWORD=

# MinIO: llave de servicio que usa el backend, acotada al bucket boletos-pdf.
# Se crea una vez con `mc admin user add` + `mc admin policy attach` (runbook, seccion 7).
MINIO_APP_ACCESS_KEY=
MINIO_APP_SECRET_KEY=

# JWT: deben ser distintos entre si y de al menos 32 caracteres, o el backend no arranca.
JWT_ACCESS_SECRET=
JWT_REFRESH_SECRET=

# Dominio publico servido por el tunel, sin https:// (ej. boletos.tudominio.com).
# Se usa para CORS_ORIGIN.
PUBLIC_HOSTNAME=

# Token del tunel de Cloudflare (Zero Trust > Networks > Tunnels > el tunel > Install).
CLOUDFLARE_TUNNEL_TOKEN=

# Solo para la siembra inicial (seccion 8 del runbook). Se pueden borrar despues.
SEED_ADMIN_EMAIL=
SEED_ADMIN_PASSWORD=
SEED_SUPER_ADMIN_EMAIL=
SEED_SUPER_ADMIN_PASSWORD=
